<Window x:Class="Mintada.Navigator.Views.ChangeCoinShapeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Mintada.Navigator.Views"
        mc:Ignorable="d"
        Title="Change Shape" Height="250" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Select Shape:" FontWeight="SemiBold" Margin="0,0,0,5"/>
        <ComboBox Grid.Row="1" x:Name="ShapesComboBox" DisplayMemberPath="Name" Margin="0,0,0,15" Padding="5"/>

        <TextBlock Grid.Row="2" Text="Shape Info (Optional):" FontWeight="SemiBold" Margin="0,0,0,5"/>
        <TextBox Grid.Row="3" x:Name="ShapeInfoTextBox" Margin="0,0,0,15" VerticalContentAlignment="Top" TextWrapping="Wrap" AcceptsReturn="True"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" IsDefault="True" Click="OkButton_Click" Width="80" Margin="0,0,10,0" Padding="5"/>
            <Button Content="Cancel" IsCancel="True" Click="CancelButton_Click" Width="80" Padding="5"/>
        </StackPanel>
    </Grid>
</Window>
